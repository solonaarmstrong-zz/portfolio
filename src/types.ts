export enum Color {
    White = 'White',
    Blue = 'Blue',
    Black = 'Black',
}

export enum Font {
    Montez = 'Montez',
    Regular = 'Regular',
}

export enum TransitionDuration {
    Quickest = 400,
    Quicker = 600,
    Quick = 800,
    Slow = 1000,
    Slower = 1200,
    Slowest = 1400
}
